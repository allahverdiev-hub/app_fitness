/** Сколько держим уведомление «Упражнение заменено» (синхрон с --replace-notice-duration-ms) */
export const REPLACE_NOTICE_MS = 2000;

/** Длительность fade-in (синхрон с --replace-notice-enter-duration) */
export const REPLACE_NOTICE_ENTER_MS = 220;

/** Длительность fade-out (синхрон с --replace-notice-leave-duration) */
export const REPLACE_NOTICE_LEAVE_MS = 180;
