export const REPS_OPTIONS = Array.from({ length: 50 }, (_, i) => i + 1);

export const WEIGHT_KG_OPTIONS = Array.from({ length: 201 }, (_, i) => i);

export const WEIGHT_GRAM_OPTIONS = Array.from({ length: 10 }, (_, i) => i * 100);
